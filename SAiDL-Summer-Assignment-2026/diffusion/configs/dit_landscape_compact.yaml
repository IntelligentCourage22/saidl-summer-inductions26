seed: 42
device: cuda

data:
  root: "/kaggle/input/landscape-pictures"
  image_size: 256
  val_fraction: 0.1
  max_train_images: 5000
  max_val_images: 1000
  num_workers: 2

vae:
  model_name: "stabilityai/sd-vae-ft-ema"
  scaling_factor: 0.18215

model:
  in_channels: 4
  latent_size: 32
  patch_size: 4
  hidden_size: 384
  depth: 6
  num_heads: 6
  mlp_ratio: 4.0
  feature_layer: 3

diffusion:
  num_timesteps: 1000
  beta_start: 0.0001
  beta_end: 0.02
  prediction_target: epsilon

training:
  batch_size: 8
  grad_accum_steps: 2
  learning_rate: 1.0e-4
  min_learning_rate: 0.0
  warmup_steps: 500
  weight_decay: 0.01
  ema_decay: 0.9999
  grad_clip: 1.0
  num_epochs: 20
  max_steps: 20000
  log_every: 100
  val_every: 1000
  max_val_batches: 16
  save_every: 1000
  sample_every: 5000
  sample_num_images: 16
  sample_batch_size: 8
  sample_ddim_steps: 100
  sample_output_dir: "results/diffusion/training_samples_compact"
  artifact_every: 0
  artifact_name: "diffusion-baseline-dit-compact-checkpoints"
  artifact_wait: true
  amp: true
  output_dir: "results/diffusion/baseline_dit_compact"
  checkpoint_dir: "checkpoints/diffusion/baseline_dit_compact"
  resume: null

sampling:
  num_images: 64
  batch_size: 8
  ddim_steps: 50
  cyclic_t_start: 200
  output_dir: "results/diffusion/samples"

predictor:
  t_mid: 500
  batch_size: 16
  learning_rate: 1.0e-4
  weight_decay: 0.01
  num_epochs: 10
  max_steps: 5000
  val_fraction: 0.1
  val_every: 100
  supervision_dir: "results/diffusion/difficulty_supervision_compact"
  output_dir: "results/diffusion/difficulty_predictor_compact"

wandb:
  enabled: true
  project: "SAiDL-Diffusion-RACD"
  entity: "ansh0-bits-pilani"
